<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions and other configurations -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizProject"
        tools:targetApi="31">

        <!-- Splash Activity (Entry point of the app) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"> <!-- Required for Android 12+ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HomeActivity (Main screen of the app) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"> <!-- Necessary if you want to navigate here from other parts of the app -->
        </activity>

        <!-- Uncomment and add HistoryActivity when ready -->
        <!-- <activity android:name=".HistoryActivity"
            android:exported="true" /> -->

    </application>

</manifest>
